name: Setup SSL (Let's Encrypt) - www.kayroslab.com

# A lancer UNE FOIS, apres que le DNS A de kayroslab.com et www.kayroslab.com
# pointe vers 51.210.9.71. Le vhost HTTP est pose par deploy-www.sh ;
# ce workflow ne fait que demander le certificat puis reapplique le vhost HTTPS.

on:
  workflow_dispatch:

jobs:
  setup-ssl-www:
    runs-on: ubuntu-latest
    steps:
      - name: Configure HTTPS for www.kayroslab.com
        uses: appleboy/ssh-action@v1.2.0
        with:
          host: 51.210.9.71
          username: ${{ secrets.VPS_SSH_USER }}
          key: ${{ secrets.VPS_SSH_KEY }}
          command_timeout: 8m
          script: |
            set -e
            git config --global --add safe.directory /opt/kayroslab
            cd /opt/kayroslab
            git fetch origin main
            git reset --hard origin/main
            find deploy/ovh-vps -name "*.sh" -exec sed -i 's/\r$//' {} \;

            apt-get update -qq
            apt-get install -y -qq certbot python3-certbot-nginx

            APP_DIR=/opt/kayroslab bash deploy/ovh-vps/deploy-www.sh

            certbot --nginx \
              -d www.kayroslab.com \
              -d kayroslab.com \
              --non-interactive \
              --agree-tos \
              --email contact@kayroslab.com \
              --redirect

            APP_DIR=/opt/kayroslab bash deploy/ovh-vps/deploy-www.sh
            nginx -t && systemctl reload nginx
            certbot certificates
            systemctl enable --now certbot.timer || true
            echo "SSL configure pour www.kayroslab.com"
